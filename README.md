# Mixin4Py
It provides a functionality similar to Java Sponge Powered Mixin to hook into the runtime classloading process for Python.It also provides a richer set of methods to support objects other than classes.But it is still in the early stages of development.
